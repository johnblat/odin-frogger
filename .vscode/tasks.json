{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
      {
        "type": "shell",
        "command": [
          "${workspaceFolder}/build_default.bat"
        ],
        "label": "[Debug] Build"
      },
      {
        "type": "shell",
        "command": [
          "${workspaceFolder}/build_hot_reload.bat"
        ],
        "label": "Build Hot Reload",
        "problemMatcher": [
          "$msCompile"
        ]
      }
    ]
}
